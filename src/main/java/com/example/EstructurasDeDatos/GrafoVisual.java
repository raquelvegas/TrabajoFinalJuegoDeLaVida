package com.example.EstructurasDeDatos;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.Group;
import javafx.scene.shape.Circle;
import javafx.scene.shape.Line;
import javafx.scene.paint.Color;
import javafx.stage.Stage;

public class GrafoVisual extends Application {

//    private Grafo grafo; // Suponiendo que tienes una instancia de Grafo

    @Override
    public void start(Stage primaryStage) {
//        Group root = new Group();
//
//        // Crear nodos para los vértices
//        for (Vertice vertice : grafo.getVertices()) {
//            Circle circle = new Circle(vertice.getX(), vertice.getY(), 10);
//            circle.setFill(Color.BLUE);
//            root.getChildren().add(circle);
//        }
//
//        // Crear nodos para las aristas
//        for (Arista arista : grafo.getAristas()) {
//            Line line = new Line(arista.getVerticeIni().getX(), arista.getVerticeIni().getY(),
//                    arista.getVerticeFin().getX(), arista.getVerticeFin().getY());
//            line.setStroke(Color.BLACK);
//            root.getChildren().add(line);
//        }
//
//        Scene scene = new Scene(root, 800, 600);
//        primaryStage.setScene(scene);
//        primaryStage.setTitle("Grafo Visual");
//        primaryStage.show();
//    }
//
//    public static void main(String[] args) {
//        launch(args);
    }
}
